<tr>
	<td><%= link_to dagger.title, dagger %></td>
	<td class="hidden-xs"><%= dagger.dagger_date.strftime("%B %d, %Y") %></td>
	<td class="visible-xs"><%= dagger.dagger_date.strftime("%m/%d/%Y") %></td>
	<td><%= dagger.author %></td>
	<td><%= link_to 'VIEW', dagger.pdf.url %></td>
</tr>